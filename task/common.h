#ifndef COMMON_H
#define COMMON_H

#include <stdio.h> 
#include <stdint.h>
#include <stdlib.h>
#include <stddef.h>
#include <stdbool.h>

#define WINDOW_SIZE 32

#define MIN(X, Y) (((X) < (Y)) ? (X) : (Y))
#define MAX(X, Y) (((X) > (Y)) ? (X) : (Y))


typedef uint16_t accel_t;


#endif

