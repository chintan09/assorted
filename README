This is the first github project, goal is to familiarize with github workflow. 

git commands: 

git commit -m 'first commit'
git remote add origin git@github.com:chintan09/assorted.git
git push origin master


todo: 
draw a circle
code for testing a mutex
linked list, stack, queue etc.
add doxygen 
mulitply all elements in an array with each other in order of N 
